import '@jest/globals';

// Test setup file
// This file runs before all tests

beforeAll(async () => {
    // Setup test database connection if needed
});

afterAll(async () => {
    // Cleanup after tests
});

